# Copy to .env and fill in values

# Set to true to enable the live-translate feature; false to hide/disable it
VITE_TRANSLATE_ENABLED=false

# Base URL of your translation proxy backend (no trailing slash)
# Leave empty if not yet deployed
VITE_TRANSLATE_PROXY_URL=

# Backend-only secret — never commit this!
# GOOGLE_API_KEY=REPLACE_WITH_YOUR_GOOGLE_API_KEY
